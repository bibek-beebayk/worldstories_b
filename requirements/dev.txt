-r base.txt

# dev.py points DEFAULT_FILE_STORAGE at S3 (R2) same as prod, so this is required
# here too — without it, any file upload (cover/pdf/epub) crashes locally.
boto3
ipdb
ipython
django-debug-toolbar
django-extensions
black
python-magic; sys_platform == 'linux'
python-magic-bin; sys_platform == 'win32' or sys_platform == 'darwin'

# used only by manage.py seed_demo_data to generate mock PDF/EPUB files
fpdf2
ebooklib